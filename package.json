{
  "name": "mesin-kasir-solo-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "latest",
    "typescript": "^5.3.3"
  },
  "packageManager": "npm@10.2.4",
  "dependencies": {
    "@google/genai": "^1.45.0",
    "motion": "^12.35.0"
  }
}
